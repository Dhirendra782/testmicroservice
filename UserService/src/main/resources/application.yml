server:
  port: 8080

spring:
  application:
    name: USER-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/testmicroserviceuserservicedb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

#Eureka CLient configuraction
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
