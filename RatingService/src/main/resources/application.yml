server:
  port: 8081

spring:
  application:
    name: RATING-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/testmicroserviceratingservicedb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

#Eureka CLient configuraction
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
